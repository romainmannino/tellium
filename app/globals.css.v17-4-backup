@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --bg-0: #02040a;
  --bg-1: #060b18;
  --ink: #e8f0ff;
  --ink-dim: #7f93bf;
  --ink-faint: rgba(150, 175, 230, 0.4);
}

html, body { height: 100%; margin: 0; }
body {
  background: radial-gradient(1200px 820px at 50% 40%, #0a1226 0%, var(--bg-1) 44%, var(--bg-0) 100%);
  color: var(--ink);
  overflow: hidden;
  -webkit-font-smoothing: antialiased;
  font-family: ui-sans-serif, -apple-system, "Segoe UI", Inter, Roboto, sans-serif;
}

/* ---------------- canvas ---------------- */
.tellium-canvas { cursor: grab; touch-action: none; }

/* ---------------- HUD générique ---------------- */
.hud { position: fixed; z-index: 10; pointer-events: none; }

.hud.top {
  inset: 0 0 auto 0;
  display: flex; align-items: flex-start; justify-content: space-between;
  padding: clamp(16px, 3vw, 34px);
}
.brand {
  font-size: clamp(13px, 1.6vw, 17px); font-weight: 300;
  text-transform: uppercase; letter-spacing: 0.42em; line-height: 1.2;
}
.brand-sub {
  display: block; margin-top: 7px; font-size: 10px; letter-spacing: 0.16em;
  color: var(--ink-dim); text-transform: none;
}
.clock {
  text-align: right; font-size: clamp(11px, 1.3vw, 13px); line-height: 1.7;
  color: var(--ink-dim); letter-spacing: 0.14em;
}
.clock b { color: var(--ink); font-weight: 500; }

/* ---------------- bloc compteur (héros) ---------------- */
.counter-block {
  left: 50%; top: clamp(84px, 14vh, 150px);
  transform: translateX(-50%); text-align: center;
  width: min(94vw, 820px);
}
.counter-shell {
  display: inline-block; position: relative; padding: 8px 30px; border-radius: 20px;
  background: radial-gradient(120% 150% at 50% 42%, rgba(12, 20, 42, 0.6), rgba(6, 11, 24, 0) 72%);
}
.odometer {
  --dh: clamp(40px, 7vw, 74px);
  display: inline-flex; align-items: stretch; gap: 1px;
  font-variant-numeric: tabular-nums;
  filter: drop-shadow(0 2px 20px rgba(90, 140, 240, 0.3));
}
.digit {
  position: relative; width: clamp(20px, 3.8vw, 42px); height: var(--dh); overflow: hidden;
  -webkit-mask-image: linear-gradient(to bottom, transparent, #000 20%, #000 80%, transparent);
          mask-image: linear-gradient(to bottom, transparent, #000 20%, #000 80%, transparent);
}
.digit.sep {
  width: clamp(10px, 1.6vw, 18px); overflow: visible;
  -webkit-mask-image: none; mask-image: none;
  display: flex; align-items: flex-end; justify-content: center;
}
.comma { font-size: clamp(28px, 5vw, 58px); font-weight: 200; color: var(--ink-faint); line-height: 1.4; }
.strip {
  position: absolute; inset: 0 0 auto 0;
  transition: transform 640ms cubic-bezier(0.22, 0.61, 0.16, 1); will-change: transform;
}
.strip span {
  display: flex; align-items: center; justify-content: center; height: var(--dh);
  font-size: clamp(30px, 5.5vw, 62px); font-weight: 200; color: #f5f9ff; line-height: 1;
}
.counter-label {
  margin-top: 14px; font-size: clamp(11px, 1.35vw, 14px); font-weight: 300;
  text-transform: uppercase; letter-spacing: 0.28em; color: var(--ink-faint);
}
.counter-stats {
  margin-top: 10px; font-size: clamp(11px, 1.4vw, 14px);
  text-transform: uppercase; letter-spacing: 0.2em; color: var(--ink-dim);
}
.counter-stats b { color: var(--ink); font-weight: 500; }
.counter-stats .dot { margin: 0 8px; }

/* ---------------- contrôles carte ---------------- */
.map-controls {
  position: fixed; z-index: 10;
  right: clamp(14px, 2.4vw, 26px); bottom: clamp(86px, 13vh, 120px);
  display: flex; flex-direction: column; gap: 8px;
}
.ctrl {
  pointer-events: auto; width: 38px; height: 38px;
  display: flex; align-items: center; justify-content: center;
  border-radius: 11px; background: rgba(16, 24, 44, 0.6);
  border: 1px solid rgba(130, 170, 255, 0.2); color: var(--ink);
  font-size: 20px; font-weight: 300; cursor: pointer;
  backdrop-filter: blur(10px); transition: border-color 0.2s, background 0.2s;
}
.ctrl:hover { border-color: rgba(150, 190, 255, 0.5); background: rgba(28, 42, 74, 0.7); }
.ctrl:active { transform: translateY(1px); }
.ctrl.find { color: #ffe6bf; border-color: rgba(255, 196, 107, 0.4); }
.ctrl.find:hover { border-color: rgba(255, 196, 107, 0.75); }
.ctrl:disabled { opacity: 0.35; cursor: default; }
.ctrl:focus-visible { outline: 2px solid #9fc0ff; outline-offset: 2px; }

/* ---------------- boutons ---------------- */
.actions {
  position: fixed; z-index: 10; bottom: clamp(20px, 4vh, 40px); left: 50%;
  transform: translateX(-50%); display: flex; flex-wrap: wrap; gap: 10px;
  justify-content: center; padding: 0 16px;
}
.btn {
  appearance: none; cursor: pointer; background: rgba(20, 30, 55, 0.55);
  border: 1px solid rgba(130, 170, 255, 0.22); color: var(--ink);
  padding: 12px 22px; border-radius: 999px; font-size: 12.5px; letter-spacing: 0.13em;
  text-transform: uppercase; font-weight: 400; backdrop-filter: blur(10px);
  transition: border-color 0.25s, background 0.25s, transform 0.12s;
}
.btn:hover { border-color: rgba(150, 190, 255, 0.55); background: rgba(30, 44, 78, 0.7); }
.btn:active { transform: translateY(1px); }
.btn:disabled { opacity: 0.5; cursor: default; }
.btn:focus-visible { outline: 2px solid #9fc0ff; outline-offset: 3px; }
.btn.primary { border-color: rgba(255, 196, 107, 0.5); color: #ffe6bf; background: rgba(60, 42, 16, 0.4); }
.btn.primary:hover { border-color: rgba(255, 196, 107, 0.85); background: rgba(80, 56, 20, 0.55); }

/* ---------------- notifications ---------------- */
.toasts {
  position: fixed; z-index: 10; bottom: clamp(84px, 12vh, 120px); left: 50%;
  transform: translateX(-50%); display: flex; flex-direction: column-reverse;
  align-items: center; gap: 8px; width: min(92vw, 520px); pointer-events: none;
}
.toast {
  background: rgba(10, 16, 32, 0.82); border: 1px solid rgba(130, 170, 255, 0.22);
  padding: 9px 16px; border-radius: 999px; font-size: 12.5px; color: var(--ink);
  backdrop-filter: blur(8px); animation: rise 0.5s cubic-bezier(0.2, 0.7, 0.2, 1) both; max-width: 100%;
}
.toast.friend { border-color: rgba(255, 119, 200, 0.5); color: #ffd7ee; }
@keyframes rise { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: none; } }

/* ---------------- fiche pays (survol) ---------------- */
.tooltip {
  position: fixed; z-index: 11; pointer-events: none;
  transform: translate(-50%, calc(-100% - 16px));
  background: rgba(9, 14, 28, 0.82); border: 1px solid rgba(130, 170, 255, 0.22);
  border-radius: 12px; padding: 10px 14px; backdrop-filter: blur(10px);
  white-space: nowrap; box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4);
  animation: ttin 0.18s ease both;
}
.tt-country { font-size: 13px; font-weight: 500; letter-spacing: 0.04em; color: var(--ink); }
.tt-row { font-size: 11.5px; color: var(--ink-dim); margin-top: 2px; }
.tt-row b { color: #dbe7ff; font-weight: 600; }
.tt-ago { font-size: 10.5px; color: var(--ink-faint); margin-top: 4px; }
@keyframes ttin {
  from { opacity: 0; transform: translate(-50%, calc(-100% - 8px)); }
  to { opacity: 1; transform: translate(-50%, calc(-100% - 16px)); }
}

/* ---------------- animation de capture ---------------- */
.capture-overlay { position: fixed; inset: 0; z-index: 20; pointer-events: none; opacity: 0; transition: opacity 0.25s; }
.capture-overlay.on { opacity: 1; }
.capture-sweep {
  position: absolute; top: 0; left: -30%; width: 30%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(190, 215, 255, 0.14), rgba(255, 255, 255, 0.5), rgba(190, 215, 255, 0.14), transparent);
  filter: blur(2px); opacity: 0;
}
.capture-overlay.on .capture-sweep { animation: sweep 1s cubic-bezier(0.4, 0, 0.2, 1) both; }
@keyframes sweep {
  0% { left: -30%; opacity: 0; }
  15% { opacity: 1; }
  85% { opacity: 1; }
  100% { left: 100%; opacity: 0; }
}
.capture-word {
  position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
  font-size: clamp(16px, 3vw, 26px); font-weight: 200; letter-spacing: 0.3em;
  text-transform: uppercase; color: #eaf1ff; opacity: 0;
}
.capture-overlay.on .capture-word { animation: capword 1s ease both; }
@keyframes capword {
  0% { opacity: 0; letter-spacing: 0.5em; }
  30% { opacity: 1; letter-spacing: 0.3em; }
  75% { opacity: 1; }
  100% { opacity: 0; }
}

/* ---------------- responsive mobile ---------------- */
@media (max-width: 640px) {
  .counter-block { top: clamp(76px, 12vh, 120px); }
  .counter-stats { letter-spacing: 0.14em; }
  .map-controls { bottom: clamp(98px, 17vh, 150px); }
  .btn { padding: 11px 16px; font-size: 11.5px; }
  .ctrl { width: 40px; height: 40px; }
}

@media (prefers-reduced-motion: reduce) {
  .strip { transition-duration: 220ms; }
  .capture-overlay.on .capture-sweep,
  .capture-overlay.on .capture-word { animation-duration: 0.4s; }
}

/* ---------------- sélecteur de mode (carte / globe) ---------------- */
.mode-toggle {
  position: fixed; z-index: 10;
  left: clamp(14px, 2.4vw, 26px); bottom: clamp(86px, 13vh, 120px);
  display: flex; padding: 4px; border-radius: 12px;
  background: rgba(16, 24, 44, 0.6); border: 1px solid rgba(130, 170, 255, 0.2);
  backdrop-filter: blur(10px);
}
.mode-toggle .thumb {
  position: absolute; top: 4px; left: 4px;
  width: calc(50% - 4px); height: calc(100% - 8px);
  border-radius: 9px; background: rgba(120, 160, 255, 0.16);
  border: 1px solid rgba(150, 190, 255, 0.35);
  transition: transform 0.32s cubic-bezier(0.4, 0, 0.2, 1); pointer-events: none;
}
.mode-toggle .seg {
  position: relative; z-index: 1; width: 40px; height: 34px;
  display: flex; align-items: center; justify-content: center;
  background: transparent; border: none; color: var(--ink-dim);
  cursor: pointer; transition: color 0.2s;
}
.mode-toggle .seg.active { color: var(--ink); }
.mode-toggle .seg:focus-visible { outline: 2px solid #9fc0ff; outline-offset: 2px; border-radius: 9px; }

@media (max-width: 640px) {
  .mode-toggle { bottom: clamp(98px, 17vh, 150px); }
}

/* =========================================================
   AFFINEMENTS "ŒUVRE NUMÉRIQUE" (art direction)
   ========================================================= */

/* --- compteur : plus léger, flottant, hypnotique --- */
.counter-shell {
  padding: 4px 18px;
  background: radial-gradient(120% 150% at 50% 42%, rgba(10, 18, 38, 0.35), rgba(6, 11, 24, 0) 70%);
}
.odometer {
  --dh: clamp(30px, 5vw, 56px);
  gap: 0;
  filter: drop-shadow(0 2px 26px rgba(120, 165, 255, 0.22));
}
.digit { width: clamp(16px, 3vw, 34px); }
.strip { transition: transform 760ms cubic-bezier(0.16, 0.7, 0.12, 1); }
.strip span { font-weight: 100; font-size: clamp(24px, 4.4vw, 50px); color: #eef4ff; }
.comma { font-weight: 100; font-size: clamp(22px, 4vw, 46px); }
.counter-label {
  margin-top: 12px; font-size: clamp(9px, 1.1vw, 11px);
  letter-spacing: 0.34em; color: rgba(150, 175, 230, 0.32);
}
.counter-stats { margin-top: 8px; font-size: clamp(10px, 1.15vw, 12px); letter-spacing: 0.16em; opacity: 0.85; }

/* --- interface presque invisible : boutons très discrets --- */
.btn {
  background: rgba(18, 26, 48, 0.3); border-color: rgba(140, 175, 255, 0.14);
  color: rgba(232, 240, 255, 0.72); padding: 10px 18px; font-size: 11px; opacity: 0.7;
  transition: opacity 0.4s, border-color 0.3s, background 0.3s, transform 0.12s;
}
.btn:hover { opacity: 1; border-color: rgba(150, 190, 255, 0.4); background: rgba(28, 40, 72, 0.55); }
.btn.primary { border-color: rgba(255, 196, 107, 0.32); color: rgba(255, 230, 191, 0.85); background: rgba(60, 42, 16, 0.28); }
.ctrl, .mode-toggle { opacity: 0.72; transition: opacity 0.4s; }
.ctrl:hover, .mode-toggle:hover { opacity: 1; }

/* --- fondu doux de toute l'UI (Gallery Mode) --- */
.hud, .actions, .map-controls, .mode-toggle, .toasts, .tooltip, .self-card {
  transition: opacity 0.85s ease;
}
.gallery .hud,
.gallery .actions,
.gallery .map-controls,
.gallery .mode-toggle,
.gallery .toasts,
.gallery .tooltip,
.gallery .self-card {
  opacity: 0 !important;
  pointer-events: none !important;
}

/* --- cadre premium (illusion de tableau) : desktop uniquement --- */
.art-frame {
  position: fixed; inset: 0; z-index: 15; pointer-events: none;
  box-shadow:
    inset 0 0 0 1px rgba(150, 180, 255, 0.06),
    inset 0 0 0 7px rgba(3, 6, 12, 0.92),
    inset 0 0 0 8px rgba(255, 255, 255, 0.045),
    inset 0 22px 70px rgba(0, 0, 0, 0.4),
    inset 0 -34px 90px rgba(0, 0, 0, 0.46);
}
.art-frame::before {
  content: ""; position: absolute; inset: 8px;
  background: linear-gradient(158deg, rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0) 28%);
}
@media (max-width: 640px) {
  .art-frame { display: none; }
}

/* --- panneau "Me" (clic sur ma lumière) --- */
.self-card {
  position: fixed; z-index: 12; transform: translate(-50%, calc(-100% - 18px));
  background: rgba(9, 14, 28, 0.86); border: 1px solid rgba(255, 200, 120, 0.25);
  border-radius: 14px; padding: 14px 16px 12px; min-width: 200px;
  backdrop-filter: blur(12px); box-shadow: 0 12px 44px rgba(0, 0, 0, 0.5);
  animation: ttin 0.2s ease both;
}
.self-card h4 {
  margin: 0 0 9px; font-size: 11.5px; letter-spacing: 0.16em; text-transform: uppercase;
  color: #ffe6bf; font-weight: 600;
}
.self-card .row { display: flex; justify-content: space-between; gap: 20px; font-size: 11.5px; color: var(--ink-dim); margin-top: 5px; }
.self-card .row b { color: #e8f0ff; font-weight: 500; }
.self-card .close {
  position: absolute; top: 7px; right: 10px; background: none; border: none;
  color: var(--ink-dim); font-size: 15px; line-height: 1; cursor: pointer; pointer-events: auto;
}
.self-card .close:hover { color: var(--ink); }

/* --- compteur remonté (ne masque plus le centre de la carte/globe) --- */
.counter-block { top: clamp(60px, 9vh, 104px); }
@media (max-width: 640px) {
  .counter-block { top: clamp(56px, 8vh, 90px); }
}

/* =========================================================
   UNIVERS OR / NUIT (direction "veilleuses de la Terre")
   ========================================================= */
:root {
  --gold: #f0b45a;
  --gold-soft: rgba(240, 190, 120, 0.78);
  --ink: #f3e6cf;
  --ink-dim: rgba(220, 190, 140, 0.6);
  --ink-faint: rgba(210, 175, 120, 0.34);
}
body {
  background: radial-gradient(1200px 900px at 50% 6%, #0c0904 0%, #070502 46%, #030201 100%);
  color: var(--ink);
}

/* --- barre du haut : horloge (gauche) · logo centré · slogan --- */
.brand {
  position: absolute; left: 50%; top: clamp(14px, 3vw, 30px); transform: translateX(-50%);
  text-align: center; letter-spacing: 0.5em; color: var(--ink); font-weight: 300;
}
.brand-sub {
  color: var(--gold-soft); letter-spacing: 0.26em; text-transform: uppercase; font-size: 9.5px; margin-top: 8px;
}
.clock { text-align: left; letter-spacing: 0.12em; }
.clock-time { font-size: clamp(14px, 1.6vw, 18px); color: var(--ink); letter-spacing: 0.1em; }
.clock-date { font-size: 10px; color: var(--ink-dim); margin-top: 3px; letter-spacing: 0.14em; }
.top-right { width: 40px; }

/* --- compteur LED (chiffres en points lumineux) --- */
.led {
  display: block; height: var(--h); width: auto; overflow: visible;
  filter: drop-shadow(0 0 6px rgba(255, 190, 110, 0.5)) drop-shadow(0 0 2px rgba(255, 224, 176, 0.85));
}
.led-hero { --h: clamp(42px, 7vw, 76px); }
.led-mini { --h: clamp(20px, 3vw, 30px); }
.led-dot { fill: #ffd98a; animation: ledtw 3.2s ease-in-out infinite; }
@keyframes ledtw {
  0%, 100% { opacity: 0.92; }
  50% { opacity: 0.6; }
}

/* --- bloc bas : label + compteur + stats + actions --- */
.stage-bottom {
  position: fixed; z-index: 10; left: 50%; bottom: clamp(18px, 3.4vh, 34px); transform: translateX(-50%);
  display: flex; flex-direction: column; align-items: center; gap: 10px;
  width: min(96vw, 900px); text-align: center; text-shadow: 0 1px 24px rgba(0, 0, 0, 0.6);
}
.stage-bottom .counter-label {
  margin: 0; font-size: clamp(9px, 1.1vw, 11px); letter-spacing: 0.34em; text-transform: uppercase; color: var(--ink-faint);
}
.stage-bottom .counter-stats {
  margin: 0; font-size: clamp(10px, 1.15vw, 12px); letter-spacing: 0.16em; text-transform: uppercase; color: var(--ink-dim);
}
.stage-bottom .counter-stats b { color: var(--ink); }
.stage-bottom .counter-stats .dot { margin: 0 8px; }
.stage-bottom .actions { position: static; transform: none; margin-top: 6px; padding: 0; }

/* --- boutons or --- */
.btn { border-color: rgba(240, 190, 120, 0.26); color: var(--gold-soft); background: rgba(30, 20, 6, 0.3); }
.btn:hover { border-color: rgba(240, 190, 120, 0.6); background: rgba(48, 32, 10, 0.5); color: #ffe9c4; }
.btn.primary { border-color: rgba(240, 190, 120, 0.5); color: #ffe6bf; background: rgba(60, 42, 16, 0.34); }
.btn.primary:hover { border-color: rgba(240, 190, 120, 0.9); background: rgba(80, 56, 20, 0.55); }

/* --- toasts remontés au-dessus du bloc bas --- */
.toasts { bottom: clamp(150px, 22vh, 220px); }

/* --- accents or sur contrôles / fiches --- */
.ctrl.find { color: #ffe6bf; border-color: rgba(240, 190, 120, 0.4); }
.tooltip { border-color: rgba(240, 190, 120, 0.22); }
.tt-row b { color: #ffe9c4; }
.self-card { border-color: rgba(240, 190, 120, 0.3); }

@media (max-width: 640px) {
  .stage-bottom { width: 94vw; bottom: clamp(14px, 3vh, 26px); }
  .brand { letter-spacing: 0.34em; }
}

/* =========================================================
   V7 — HOME LED + REVEAL + ARTWORK
   ========================================================= */
.tellium-scene { background: #030201; isolation: isolate; }
.tellium-canvas { position: fixed; inset: 0; z-index: 1; transition: opacity 1.1s ease, transform 2.1s cubic-bezier(.18,.75,.18,1), filter 1.4s ease; }
.scene-home .tellium-canvas { opacity: .05; transform: translateY(30vh) scale(.9); filter: blur(3px) brightness(.35); pointer-events: none; }
.scene-revealing .tellium-canvas { opacity: 1; transform: translateY(0) scale(1); filter: none; pointer-events: none; }
.scene-artwork .tellium-canvas { opacity: 1; transform: none; filter: none; }
.space-veil { position: fixed; inset: 0; z-index: 2; pointer-events: none; background: radial-gradient(circle at 50% 38%, transparent 0 20%, rgba(3,2,1,.08) 48%, rgba(3,2,1,.48) 100%); }

.persistent-clock { position: fixed; top: clamp(17px,2.7vw,32px); left: clamp(18px,3vw,38px); z-index: 30; display:flex; gap:9px; align-items:flex-start; color:#f1c783; letter-spacing:.1em; }
.clock-icon { font-size:18px; line-height:1.1; opacity:.9; }
.persistent-clock .clock-time { font-size:clamp(13px,1.4vw,17px); color:#f8d9a3; }
.persistent-clock .clock-date { font-size:9px; margin-top:3px; color:rgba(235,190,120,.62); letter-spacing:.15em; }
.center-brand { position:fixed; top:clamp(22px,3vw,40px); left:50%; z-index:30; transform:translateX(-50%); text-align:center; width:min(86vw,760px); pointer-events:none; transition:opacity .7s ease, transform .8s ease; }
.brand-name { text-transform:uppercase; letter-spacing:.55em; font-size:clamp(18px,2.3vw,32px); font-weight:300; color:#f4d4a0; text-shadow:0 0 24px rgba(255,190,95,.24); }
.brand-tagline { margin-top:9px; text-transform:uppercase; letter-spacing:.3em; font-size:clamp(7px,.8vw,10px); color:rgba(238,188,105,.72); }

.home-stage { position:fixed; inset:0; z-index:12; display:grid; place-items:center; padding-top:3vh; transition:opacity .9s ease, transform 1.5s cubic-bezier(.18,.75,.18,1); }
.scene-revealing .home-stage { opacity:0; transform:translateY(-11vh) scale(.82); pointer-events:none; }
.scene-artwork .home-stage { opacity:0; pointer-events:none; visibility:hidden; }
.home-orb { position:relative; width:min(67vw,67vh,760px); aspect-ratio:1; border-radius:50%; display:flex; flex-direction:column; align-items:center; justify-content:center; background:radial-gradient(circle at 50% 38%, rgba(35,22,8,.28), rgba(4,3,2,.93) 64%, #010101 100%); border:1px solid rgba(245,182,82,.68); box-shadow:0 0 0 7px rgba(0,0,0,.45), 0 0 38px rgba(255,164,45,.24), inset 0 0 70px rgba(255,151,31,.05); overflow:hidden; }
.home-orb::before { content:""; position:absolute; inset:-1px; border-radius:50%; background:conic-gradient(from 210deg, transparent 0 18%, rgba(255,188,86,.8) 24%, transparent 31% 62%, rgba(255,218,155,.95) 68%, transparent 74%); padding:1px; -webkit-mask:linear-gradient(#000 0 0) content-box,linear-gradient(#000 0 0); -webkit-mask-composite:xor; mask-composite:exclude; }
.orb-grid { position:absolute; inset:8%; opacity:.34; background-image:radial-gradient(circle, rgba(180,116,46,.42) 0 1px, transparent 1.35px); background-size:14px 14px; -webkit-mask-image:radial-gradient(circle,#000 45%,transparent 86%); mask-image:radial-gradient(circle,#000 45%,transparent 86%); }
.home-counter { position:relative; z-index:2; width:76%; display:flex; flex-direction:column; align-items:center; margin-top:-3%; }
.home-counter .led-hero { width:100%; max-height:23vh; filter:drop-shadow(0 0 17px rgba(255,181,80,.6)); }
.home-counter-label { margin-top:18px; text-transform:uppercase; letter-spacing:.3em; font-size:clamp(8px,1vw,12px); color:rgba(241,191,116,.78); }
.reveal-button { position:relative; z-index:3; margin-top:clamp(22px,4vh,42px); width:clamp(116px,12vw,150px); aspect-ratio:1; border-radius:50%; border:1px solid rgba(245,183,87,.78); background:radial-gradient(circle,rgba(48,28,8,.42),rgba(6,4,2,.84)); color:#f5cf94; text-transform:uppercase; letter-spacing:.24em; font-size:clamp(8px,.8vw,10px); line-height:1.8; cursor:pointer; box-shadow:0 0 28px rgba(255,169,52,.18), inset 0 0 24px rgba(255,164,48,.06); transition:transform .25s ease, box-shadow .25s ease; }
.reveal-button:hover { transform:scale(1.045); box-shadow:0 0 42px rgba(255,169,52,.34), inset 0 0 28px rgba(255,164,48,.1); }
.reveal-button span { display:block; }
.reveal-button b { display:block; font-size:18px; font-weight:300; margin-top:3px; }

.rising-earth { position:fixed; z-index:8; left:-8vw; right:-8vw; bottom:-29vh; height:52vh; border-radius:50% 50% 0 0 / 32% 32% 0 0; pointer-events:none; background:radial-gradient(ellipse at 50% 0%, rgba(255,223,170,.8) 0 1px, rgba(255,168,66,.25) 2px, transparent 11%), radial-gradient(ellipse at 50% 12%, rgba(19,14,8,.2), rgba(2,2,2,.97) 62%); border-top:1px solid rgba(218,231,255,.8); box-shadow:0 -2px 18px rgba(92,165,255,.42),0 -10px 55px rgba(255,145,37,.16); transition:bottom 2s cubic-bezier(.18,.75,.18,1),height 2s cubic-bezier(.18,.75,.18,1),opacity .7s ease; }
.scene-revealing .rising-earth { bottom:-6vh; height:86vh; opacity:.28; }
.scene-artwork .rising-earth { opacity:0; }

.transition-field { position:fixed; inset:0; z-index:20; pointer-events:none; }
.transition-field i { position:absolute; display:block; border-radius:50%; background:#ffe4b3; box-shadow:0 0 9px #ffb24f,0 0 20px rgba(255,153,40,.65); animation:star-disperse 1.65s cubic-bezier(.18,.75,.2,1) forwards; }
@keyframes star-disperse { 0%{opacity:0;transform:translate(0,0) scale(.3)} 12%{opacity:1} 68%{opacity:1} 100%{opacity:0;transform:translate(var(--tx),var(--ty)) scale(1.4)} }

.scene-artwork .center-brand { transform:translateX(-50%) scale(.88); opacity:.88; }
.artwork-bottom { left:50%; bottom:clamp(12px,2.4vh,28px); transform:translateX(-50%); width:min(94vw,760px); text-align:center; z-index:25; }
.artwork-counter-label { text-transform:uppercase; letter-spacing:.26em; color:rgba(240,190,115,.72); font-size:9px; }
.artwork-bottom .led-mini { width:min(34vw,280px); height:auto; margin:7px auto 0; filter:drop-shadow(0 0 9px rgba(255,180,80,.32)); }
.artwork-meta { margin-top:5px; text-transform:uppercase; letter-spacing:.17em; font-size:8px; color:rgba(226,184,119,.46); }
.artwork-actions { position:static; transform:none; margin-top:10px; padding:0; }
.artwork-actions .btn { opacity:.88; border-color:rgba(244,184,86,.4); background:rgba(18,11,4,.55); color:#f3d5a5; }

.scene-home .map-controls,.scene-home .mode-toggle,.scene-revealing .map-controls,.scene-revealing .mode-toggle { opacity:0; pointer-events:none; }
.scene-home .toasts,.scene-revealing .toasts { opacity:0; }

@media (max-width: 700px) {
  .persistent-clock { top:14px; left:14px; transform:scale(.9); transform-origin:top left; }
  .center-brand { top:18px; }
  .brand-name { font-size:17px; }
  .brand-tagline { font-size:6px; letter-spacing:.18em; }
  .home-stage { padding-top:5vh; }
  .home-orb { width:min(88vw,58vh); }
  .home-counter .led-hero { width:88%; }
  .reveal-button { width:98px; margin-top:18px; }
  .rising-earth { left:-28vw; right:-28vw; }
  .artwork-bottom { bottom:10px; }
  .artwork-bottom .led-mini { width:min(52vw,230px); }
  .artwork-actions { gap:7px; }
  .artwork-actions .btn { padding:9px 12px; font-size:9px; }
}

/* ================================================================
   TELLIUM V8 — BLUE HORIZON / LIVING CONSTELLATION / REAL ACTIONS
   ================================================================ */
.tellium-scene {
  --tellium-gold: #f2bd6b;
  --tellium-ivory: #fff0d1;
  --tellium-blue: #4b9dff;
  --tellium-deep-blue: #071126;
  background:
    radial-gradient(900px 520px at 50% 46%, rgba(20,45,92,.16), transparent 68%),
    linear-gradient(180deg,#010102 0%,#030405 52%,#010102 100%);
}

.cosmic-blue {
  position: fixed;
  inset: 0;
  z-index: 3;
  pointer-events: none;
  background:
    radial-gradient(ellipse at 50% 92%, rgba(58,133,238,.10), transparent 42%),
    radial-gradient(circle at 50% 43%, rgba(22,56,115,.055), transparent 50%);
  mix-blend-mode: screen;
}

.scene-home .space-veil {
  z-index: 5;
  background:
    radial-gradient(circle at 50% 42%, rgba(4,8,18,.12) 0 24%, rgba(1,3,8,.72) 64%, rgba(0,1,4,.96) 100%),
    linear-gradient(180deg,rgba(0,2,7,.25),rgba(0,2,8,.72));
}
.scene-revealing .space-veil {
  background: radial-gradient(circle at 50% 46%, rgba(12,30,65,.08), rgba(1,3,9,.45) 72%);
  transition: background 1.8s ease;
}
.scene-artwork .space-veil {
  background: radial-gradient(circle at 50% 43%, transparent 0 27%, rgba(0,1,3,.28) 72%, rgba(0,0,1,.68) 100%);
}

.persistent-clock {
  color: #bcd8ff;
  text-shadow: 0 0 18px rgba(63,140,255,.32);
}
.persistent-clock .clock-time { color: #e8f2ff; }
.persistent-clock .clock-date { color: rgba(144,187,244,.72); }
.center-brand .brand-name,
.brand-name {
  color: #f6d7a2;
  text-shadow: 0 0 22px rgba(236,174,78,.18);
}
.brand-tagline { color: rgba(166,195,234,.72) !important; }

.home-orb {
  width: min(63vw,64vh,700px);
  background:
    radial-gradient(circle at 50% 36%, rgba(19,43,88,.22), transparent 42%),
    radial-gradient(circle at 50% 55%, rgba(13,19,36,.75), rgba(1,3,8,.98) 72%);
  border-color: rgba(244,188,102,.68);
  box-shadow:
    0 0 0 1px rgba(83,151,245,.12),
    0 0 52px rgba(60,132,235,.17),
    0 0 34px rgba(255,170,60,.12),
    inset 0 0 90px rgba(41,91,171,.08);
}
.home-orb::after {
  content:"";
  position:absolute;
  inset:6%;
  border-radius:50%;
  pointer-events:none;
  background:
    radial-gradient(circle at 30% 24%,rgba(108,173,255,.28) 0 1px,transparent 1.4px),
    radial-gradient(circle at 67% 36%,rgba(255,208,134,.22) 0 1px,transparent 1.5px),
    radial-gradient(circle at 46% 70%,rgba(92,155,240,.2) 0 1px,transparent 1.4px);
  background-size: 29px 31px,37px 33px,43px 39px;
  opacity:.34;
  mask-image:radial-gradient(circle,#000 42%,transparent 82%);
}
.home-counter { z-index:4; }
.home-counter .led-hero {
  filter:
    drop-shadow(0 0 9px rgba(255,196,100,.68))
    drop-shadow(0 0 28px rgba(75,145,244,.2));
}
.home-counter-label { color:rgba(222,229,241,.68) !important; }
.reveal-button {
  border-color:rgba(241,190,103,.76);
  background:radial-gradient(circle at 50% 35%,rgba(35,73,126,.23),rgba(5,8,15,.92) 65%);
  box-shadow:0 0 32px rgba(70,143,244,.14),0 0 22px rgba(255,169,52,.16),inset 0 0 28px rgba(69,135,225,.08);
}
.reveal-button:hover {
  box-shadow:0 0 52px rgba(70,143,244,.26),0 0 35px rgba(255,169,52,.25),inset 0 0 30px rgba(69,135,225,.12);
}

.rising-earth {
  background:
    radial-gradient(ellipse at 50% 0%,rgba(255,238,199,.9) 0 1px,rgba(89,165,255,.34) 2px,transparent 10%),
    radial-gradient(ellipse at 50% 8%,rgba(22,53,102,.38),rgba(3,8,19,.97) 62%),
    repeating-radial-gradient(ellipse at 50% 14%,rgba(80,146,225,.13) 0 1px,transparent 2px 15px);
  border-top-color:rgba(125,188,255,.88);
  box-shadow:
    0 -2px 15px rgba(127,190,255,.72),
    0 -12px 65px rgba(54,128,235,.38),
    0 -5px 28px rgba(255,173,67,.18);
}
.scene-revealing .rising-earth { opacity:.5; }
.transition-field i {
  background:#fff1d1;
  box-shadow:0 0 8px #ffc46d,0 0 19px rgba(72,144,255,.68),0 0 35px rgba(42,102,210,.32);
}

.artwork-bottom {
  width:min(94vw,800px);
  padding:10px 18px 6px;
  border-radius:22px;
  background:linear-gradient(180deg,rgba(2,6,15,.04),rgba(2,6,15,.5));
  backdrop-filter:blur(5px);
}
.artwork-counter-label { color:rgba(173,202,239,.68) !important; }
.artwork-meta { color:rgba(142,179,226,.55) !important; }
.artwork-bottom .led-mini {
  filter:drop-shadow(0 0 9px rgba(255,185,88,.42)) drop-shadow(0 0 22px rgba(64,139,244,.17));
}
.actions .btn {
  border-color:rgba(101,157,235,.28);
  background:rgba(3,8,19,.62);
  color:#dbeaff;
  box-shadow:inset 0 0 18px rgba(53,118,211,.05);
}
.actions .btn.primary {
  border-color:rgba(241,181,88,.52);
  color:#f6d29a;
  box-shadow:0 0 20px rgba(238,168,57,.08),inset 0 0 18px rgba(238,168,57,.05);
}
.actions .btn span { margin-right:7px; color:#f2bd6b; }

/* Dialogues fonctionnels */
.dialog-backdrop {
  position:fixed;
  inset:0;
  z-index:100;
  display:grid;
  place-items:center;
  padding:20px;
  background:rgba(0,2,8,.76);
  backdrop-filter:blur(16px);
  animation:dialogFade .26s ease both;
}
.tellium-dialog {
  position:relative;
  width:min(92vw,560px);
  padding:34px;
  border:1px solid rgba(102,164,248,.22);
  border-radius:24px;
  color:#eaf2ff;
  background:
    radial-gradient(circle at 50% 0%,rgba(44,94,170,.18),transparent 42%),
    linear-gradient(155deg,rgba(7,15,31,.98),rgba(2,5,13,.98));
  box-shadow:0 30px 100px rgba(0,0,0,.65),0 0 55px rgba(50,119,225,.13),inset 0 0 50px rgba(255,186,82,.025);
  text-align:center;
  animation:dialogRise .38s cubic-bezier(.2,.8,.2,1) both;
}
.dialog-close {
  position:absolute;
  right:15px;
  top:13px;
  width:34px;
  height:34px;
  border-radius:50%;
  border:1px solid rgba(130,170,225,.18);
  background:rgba(3,8,18,.6);
  color:#c9dcf7;
  font-size:22px;
  cursor:pointer;
}
.dialog-star { font-size:27px; color:#f4c579; text-shadow:0 0 22px rgba(245,184,82,.72); }
.dialog-kicker { margin:12px 0 6px; color:#f0be72; text-transform:uppercase; letter-spacing:.2em; font-size:9px; }
.tellium-dialog h2 { margin:6px 0 10px; font-weight:300; letter-spacing:.05em; font-size:clamp(22px,4vw,34px); }
.tellium-dialog > p:not(.dialog-kicker) { color:rgba(194,213,240,.68); line-height:1.65; font-size:13px; }
.invite-link-row { display:flex; gap:8px; margin-top:22px; }
.invite-link-row input {
  flex:1;
  min-width:0;
  border:1px solid rgba(111,163,234,.2);
  border-radius:12px;
  background:rgba(1,4,11,.7);
  color:#cfe2ff;
  padding:13px 14px;
  outline:none;
}
.invite-link-row button,.capture-actions button,.share-shortcuts a {
  border:1px solid rgba(232,177,92,.38);
  border-radius:12px;
  background:rgba(31,21,8,.52);
  color:#f2ca8e;
  padding:12px 17px;
  cursor:pointer;
  text-decoration:none;
  font-size:11px;
  text-transform:uppercase;
  letter-spacing:.1em;
}
.share-shortcuts { display:flex; justify-content:center; gap:10px; margin-top:12px; }
.capture-dialog { width:min(96vw,1080px); padding:26px; }
.capture-preview {
  overflow:hidden;
  margin:18px auto;
  border:1px solid rgba(106,163,240,.18);
  background:#010309;
  box-shadow:0 18px 60px rgba(0,0,0,.5);
}
.capture-preview img { display:block; width:100%; max-height:58vh; object-fit:contain; }
.capture-actions { display:flex; justify-content:center; gap:12px; }
.capture-actions .primary { background:rgba(46,80,132,.35); border-color:rgba(108,169,250,.45); color:#e6f1ff; }
.capture-dialog small { display:block; margin-top:12px; color:rgba(137,176,228,.5); letter-spacing:.12em; }
@keyframes dialogFade { from{opacity:0} to{opacity:1} }
@keyframes dialogRise { from{opacity:0;transform:translateY(14px) scale(.98)} to{opacity:1;transform:none} }

@media (max-width:640px) {
  .home-orb { width:min(91vw,59vh); }
  .persistent-clock { z-index:40; }
  .center-brand { width:72vw; }
  .brand-tagline { font-size:7px !important; }
  .tellium-dialog { padding:26px 18px; }
  .invite-link-row { flex-direction:column; }
  .capture-actions { flex-direction:column; }
  .artwork-bottom { padding-bottom:2px; }
  .artwork-actions { gap:7px !important; }
  .artwork-actions .btn { padding:10px 12px !important; font-size:8px !important; }
}

/* ================================================================
   TELLIUM V9 — REAL EARTH / HONEST LIGHTS / SIMULATION LAB
   ================================================================ */
.tellium-scene {
  background:
    radial-gradient(780px 420px at 50% 45%, rgba(34,30,24,.18), transparent 68%),
    linear-gradient(180deg,#020204 0%,#050506 54%,#010102 100%);
}
.cosmic-blue {
  opacity:.28;
  background:
    radial-gradient(ellipse at 50% 96%, rgba(49,117,203,.12), transparent 34%),
    radial-gradient(circle at 50% 43%, rgba(25,50,83,.055), transparent 50%);
}
.scene-artwork .cosmic-blue { opacity:.17; }
.space-veil { background:radial-gradient(circle at 50% 42%,transparent 0 35%,rgba(0,0,0,.12) 72%,rgba(0,0,0,.58) 100%); }

/* La Terre basse est désormais une vraie image artistique issue du concept validé. */
.rising-earth {
  left:-3vw;
  right:-3vw;
  bottom:-8vh;
  height:42vh;
  border:0;
  border-radius:0;
  background-image:
    linear-gradient(180deg,transparent 0%,rgba(0,0,0,.02) 45%,rgba(0,0,0,.58) 100%),
    url('/earth-horizon.webp');
  background-size:cover;
  background-position:center top;
  background-repeat:no-repeat;
  box-shadow:none;
  opacity:.96;
  filter:saturate(.9) contrast(1.08);
  transform-origin:50% 100%;
  transition:transform 2.15s cubic-bezier(.16,.78,.16,1),opacity .8s ease,filter 1.4s ease;
}
.scene-revealing .rising-earth {
  bottom:-8vh;
  height:42vh;
  opacity:.9;
  transform:translateY(-25vh) scale(1.88);
  filter:saturate(.84) contrast(1.12) brightness(.8);
}
.scene-artwork .rising-earth { opacity:0; transform:translateY(-36vh) scale(2.25); }

.home-orb {
  background:radial-gradient(circle at 50% 40%,rgba(34,25,14,.2),rgba(3,3,4,.97) 72%);
  box-shadow:0 0 0 1px rgba(255,195,105,.1),0 0 36px rgba(255,170,62,.12),inset 0 0 72px rgba(255,172,66,.035);
}
.orb-grid { opacity:.22; }
.brand-tagline { color:rgba(220,190,145,.67)!important; }
.persistent-clock { color:#d9e8fa;text-shadow:0 0 12px rgba(76,143,220,.14); }
.persistent-clock .clock-time { color:#eef4fb; }
.persistent-clock .clock-date { color:rgba(139,174,215,.7); }

/* Correctif majeur : le parent HUD ne neutralise plus ses boutons. */
.artwork-bottom { pointer-events:auto!important; }
.artwork-actions,.artwork-actions .btn { pointer-events:auto!important; position:relative; z-index:80; }
.artwork-actions .btn { background:rgba(7,7,8,.76); }
.dialog-backdrop { z-index:200!important; pointer-events:auto!important; }

/* Laboratoire visuel, volontairement discret et supprimable avant production. */
.simulation-lab {
  position:fixed;
  right:18px;
  top:50%;
  z-index:160;
  transform:translateY(-50%);
  display:flex;
  align-items:flex-start;
  gap:8px;
  font-family:ui-sans-serif,-apple-system,"Segoe UI",sans-serif;
}
.lab-toggle {
  width:42px;height:42px;border-radius:50%;
  border:1px solid rgba(222,177,108,.34);
  background:rgba(5,5,7,.82);color:rgba(241,205,151,.78);
  font-size:9px;letter-spacing:.12em;cursor:pointer;
  backdrop-filter:blur(10px);
}
.lab-panel {
  width:250px;padding:15px 16px;border-radius:15px;
  border:1px solid rgba(222,177,108,.22);
  background:rgba(4,4,6,.9);box-shadow:0 18px 55px rgba(0,0,0,.45);
  backdrop-filter:blur(16px);
}
.lab-title { font-size:9px;text-transform:uppercase;letter-spacing:.18em;color:rgba(240,203,146,.58); }
.lab-value { margin-top:7px;font-size:25px;font-weight:300;letter-spacing:.08em;color:#f3d6a7; }
.lab-panel input[type="range"] { width:100%;margin:12px 0 10px;accent-color:#dca653; }
.lab-presets { display:grid;grid-template-columns:repeat(3,1fr);gap:5px; }
.lab-presets button { border:1px solid rgba(210,168,104,.17);background:rgba(255,255,255,.025);color:rgba(230,211,181,.72);border-radius:8px;padding:6px 4px;font-size:9px;cursor:pointer; }
.lab-presets button:hover { border-color:rgba(230,179,99,.55);color:#fff0d7; }
.lab-mode { margin-top:10px;color:rgba(151,176,207,.64);font-size:9px;line-height:1.4; }

@media (max-width:700px) {
  .rising-earth { left:-36vw;right:-36vw;height:34vh;bottom:-2vh; }
  .scene-revealing .rising-earth { transform:translateY(-24vh) scale(2.15); }
  .simulation-lab { right:10px;top:auto;bottom:88px;transform:none; }
  .lab-panel { width:min(72vw,245px); }
}

/* ================================================================
   TELLIUM V11 — ONE EARTH TRANSITION
   Les particules du compteur rejoignent de vraies positions terrestres.
   Aucun nuage aléatoire ni changement brutal de scène.
   ================================================================ */
.scene-revealing .tellium-canvas {
  animation: artworkReveal 2.7s cubic-bezier(.16,.78,.16,1) both;
}
@keyframes artworkReveal {
  0% { opacity:0; transform:translateY(23vh) scale(.84); filter:blur(5px) brightness(.32); }
  32% { opacity:.16; filter:blur(3px) brightness(.5); }
  74% { opacity:.72; }
  100% { opacity:1; transform:translateY(0) scale(1); filter:none; }
}
.scene-revealing .home-stage {
  transition-duration:1.55s;
  opacity:0;
  transform:translateY(-7vh) scale(.9);
}
.transition-field { overflow:hidden; }
.transition-field i {
  animation:land-disperse 2.35s cubic-bezier(.18,.72,.18,1) forwards;
  will-change:transform,opacity;
}
@keyframes land-disperse {
  0% { opacity:0; transform:translate(0,0) scale(.25); }
  10% { opacity:1; }
  36% { opacity:.95; }
  82% { opacity:.78; }
  100% { opacity:0; transform:translate(calc(var(--tx) - var(--sx)),calc(var(--ty) - var(--sy))) scale(.72); }
}

/* ===== V12 — One continuous Earth transition ===== */
.scene-home .tellium-canvas {
  opacity: 0;
  transform: scale(.82) translateY(7vh);
  filter: blur(2px) brightness(.45);
  pointer-events: none;
}
.scene-revealing .tellium-canvas {
  opacity: 1;
  transform: scale(1.04) translateY(0);
  filter: none;
  transition: opacity .75s ease, transform 1.45s cubic-bezier(.18,.78,.18,1), filter .8s ease;
  pointer-events: none;
}
.scene-artwork .tellium-canvas {
  opacity: 1;
  transform: scale(1);
  transition: transform .55s ease;
}
.scene-revealing .home-stage {
  opacity: 0;
  transform: scale(1.34);
  filter: blur(5px);
  transition: opacity .65s ease, transform 1.15s cubic-bezier(.2,.75,.2,1), filter .7s ease;
}
.scene-revealing .rising-earth,
.scene-artwork .rising-earth {
  opacity: 0 !important;
  transform: none !important;
}
.transition-field { display:none !important; }
.home-counter.self-arrival .led-counter {
  animation: self-number-arrival 1.55s cubic-bezier(.18,.8,.2,1);
}
.home-counter.self-arrival::after {
  content:"+1 · YOUR LIGHT";
  position:absolute;
  left:50%;
  top:-1.1rem;
  transform:translateX(-50%);
  color:#f4d49a;
  font-size:.64rem;
  letter-spacing:.22em;
  opacity:0;
  animation:self-arrival-label 1.65s ease;
  white-space:nowrap;
}
@keyframes self-number-arrival {
  0% { filter:brightness(.8); transform:scale(.985); }
  35% { filter:brightness(1.8) drop-shadow(0 0 22px rgba(255,190,90,.55)); transform:scale(1.035); }
  100% { filter:none; transform:scale(1); }
}
@keyframes self-arrival-label {
  0%,100% { opacity:0; transform:translate(-50%,8px); }
  25%,72% { opacity:.9; transform:translate(-50%,0); }
}

/* ===== V14 — Personal light, safe ticker, premium artwork ===== */
.life-ticker {
  position: fixed;
  left: 0;
  right: 0;
  bottom: max(88px, env(safe-area-inset-bottom, 0px) + 74px);
  z-index: 27;
  height: 34px;
  overflow: hidden;
  pointer-events: none;
  background: linear-gradient(90deg, rgba(1,2,4,.94), rgba(1,2,4,.24) 12%, rgba(1,2,4,.24) 88%, rgba(1,2,4,.94));
  border-top: 1px solid rgba(255,255,255,.035);
  border-bottom: 1px solid rgba(255,255,255,.025);
  mask-image: linear-gradient(90deg, transparent, #000 7%, #000 93%, transparent);
}
.life-ticker-track {
  width: max-content;
  height: 100%;
  display: flex;
  align-items: center;
  gap: 54px;
  padding-left: 100vw;
  animation: telliumTicker 42s linear infinite;
  white-space: nowrap;
}
.life-ticker span {
  font-size: 10px;
  letter-spacing: .16em;
  text-transform: uppercase;
  color: rgba(231,235,241,.7);
}
.life-ticker b { color: #e4b76f; margin-right: 10px; font-weight: 400; }
@keyframes telliumTicker { to { transform: translateX(-50%); } }

.profile-dialog { width: min(92vw, 520px); }
.profile-name {
  width: 100%; box-sizing: border-box; margin-top: 18px; padding: 14px 16px;
  border: 1px solid rgba(231,184,109,.25); border-radius: 12px;
  background: rgba(0,0,0,.32); color: #eef2f6; outline: none;
  font-size: 14px; letter-spacing: .04em;
}
.profile-name:focus { border-color: rgba(239,191,114,.6); box-shadow: 0 0 0 3px rgba(239,191,114,.08); }
.photo-picker {
  margin: 14px auto 2px; width: 96px; height: 96px; border-radius: 50%;
  display: grid; place-items: center; overflow: hidden; cursor: pointer;
  border: 1px dashed rgba(142,176,219,.35); color: rgba(193,210,232,.65);
  background: rgba(6,10,18,.55); font-size: 9px; line-height: 1.4; padding: 10px; box-sizing: border-box;
}
.photo-picker input { display: none; }
.photo-picker img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
.profile-actions { margin-top: 22px; }

.capture-dialog { width: min(96vw, 1180px); }
.capture-preview { border-radius: 4px; border-color: rgba(230,181,108,.24); background: #010203; }
.capture-preview img { max-height: 64vh; }

@media (max-width: 640px) {
  .life-ticker { bottom: max(104px, env(safe-area-inset-bottom, 0px) + 92px); height: 30px; }
  .life-ticker-track { gap: 34px; animation-duration: 34s; }
  .life-ticker span { font-size: 8px; }
  .artwork-bottom { bottom: 8px; }
}

/* ===== V15 — Signature globe, actions hautes, ticker sous compteur ===== */
.artwork-top-actions {
  position: fixed;
  top: clamp(20px, 3.2vh, 34px);
  right: clamp(20px, 2.8vw, 38px);
  z-index: 40;
  display: flex;
  align-items: center;
  gap: 10px;
  pointer-events: auto;
}
.artwork-top-actions .btn {
  opacity: .92;
  padding: 10px 17px;
  background: rgba(3, 7, 14, .66);
  border-color: rgba(218, 175, 105, .28);
  box-shadow: 0 10px 34px rgba(0,0,0,.22);
}
.artwork-top-actions .btn:hover { opacity: 1; }

.artwork-bottom {
  position: fixed;
  left: 50%;
  bottom: clamp(20px, 3vh, 34px);
  transform: translateX(-50%);
  width: min(88vw, 820px);
  z-index: 35;
  padding: 12px 24px 10px;
  border-radius: 28px;
  background: linear-gradient(180deg, rgba(4,8,15,.08), rgba(4,8,15,.76) 52%, rgba(2,4,8,.9));
  box-shadow: 0 -24px 70px rgba(0,0,0,.18);
  pointer-events: none;
}
.artwork-bottom .led-mini,
.artwork-bottom .artwork-counter-label,
.artwork-bottom .artwork-meta,
.artwork-bottom .life-ticker { pointer-events: none; }

.life-ticker {
  position: relative;
  left: auto;
  right: auto;
  bottom: auto;
  width: 100%;
  height: 30px;
  margin-top: 10px;
  z-index: 1;
  overflow: hidden;
  background: transparent;
  border-top: 1px solid rgba(255,255,255,.045);
  border-bottom: 0;
  mask-image: linear-gradient(90deg, transparent, #000 8%, #000 92%, transparent);
}
.life-ticker-track {
  gap: 48px;
  padding-left: 88%;
  animation-duration: 46s;
}
.life-ticker span {
  font-size: 9px;
  color: rgba(232,236,242,.67);
}

.profile-dialog { width: min(92vw, 500px); }
.profile-dialog > p:not(.dialog-kicker) { max-width: 410px; margin-left: auto; margin-right: auto; }
.profile-actions { display:flex; gap:10px; justify-content:center; }
.profile-actions button { min-width: 170px; }
.photo-picker { display:none !important; }

.capture-dialog { width: min(95vw, 1320px); }
.capture-preview {
  background: #010204;
  border: 1px solid rgba(230,181,108,.22);
  border-radius: 12px;
  padding: 10px;
  box-shadow: 0 26px 90px rgba(0,0,0,.45);
}
.capture-preview img { max-height: 68vh; width:100%; object-fit:contain; }

@media (max-width: 760px) {
  .artwork-top-actions {
    top: 72px;
    right: 10px;
    gap: 6px;
  }
  .artwork-top-actions .btn { padding: 8px 10px; font-size: 8px; }
  .artwork-bottom { width: calc(100vw - 20px); bottom: 8px; padding: 10px 14px 8px; }
  .life-ticker { height: 27px; margin-top: 7px; }
  .life-ticker-track { gap: 30px; animation-duration: 36s; }
  .profile-actions { flex-direction:column; }
  .profile-actions button { width:100%; }
}

/* ===== V16 — globe-only capture and social preview ===== */
.mode-toggle { display: none !important; }
.social-share-row {
  display:flex;
  justify-content:center;
  flex-wrap:wrap;
  gap:8px;
  margin-top:12px;
}
.social-share-row button {
  border:1px solid rgba(155,181,217,.22);
  background:rgba(6,12,22,.78);
  color:rgba(226,233,243,.86);
  border-radius:999px;
  padding:9px 16px;
  font-size:10px;
  letter-spacing:.1em;
  text-transform:uppercase;
}
.social-share-row button:hover { border-color:rgba(229,182,109,.55); color:#f0c27c; }
.capture-dialog small { text-align:center; }

/* ===== V17 — Real presence responsive layout ===== */

/* Le compteur ne grandit plus démesurément quand il affiche 1 seul chiffre. */
.artwork-bottom .led-mini {
  width: auto !important;
  height: clamp(72px, 13vh, 118px) !important;
  max-width: min(52vw, 360px) !important;
  display: block;
  margin: 4px auto 0;
}

/* Bloc inférieur plus compact pour laisser respirer le globe. */
.artwork-bottom {
  width: min(74vw, 680px);
  padding: 10px 22px 8px;
  bottom: clamp(12px, 2vh, 24px);
}

/* Le globe reste l'élément principal. */
.scene-artwork .tellium-canvas {
  transform: scale(.94);
  transform-origin: 50% 43%;
}

/* Affichage mobile portrait. */
@media (max-width: 760px) {
  .persistent-clock {
    top: 12px !important;
    left: 12px !important;
    transform: scale(.76) !important;
    transform-origin: top left;
  }

  .center-brand {
    top: 18px !important;
    width: 48vw !important;
  }

  .brand-name {
    font-size: 15px !important;
    letter-spacing: .32em !important;
  }

  .brand-tagline {
    display: none;
  }

  .artwork-top-actions {
    top: auto !important;
    right: auto !important;
    left: 50% !important;
    bottom: calc(env(safe-area-inset-bottom, 0px) + 14px) !important;
    transform: translateX(-50%);
    width: calc(100vw - 24px);
    justify-content: center;
    z-index: 80;
  }

  .artwork-top-actions .btn {
    flex: 1;
    max-width: 170px;
    padding: 9px 8px !important;
    font-size: 7.5px !important;
    white-space: nowrap;
  }

  .scene-artwork .tellium-canvas {
    transform: scale(.82) translateY(-5vh);
    transform-origin: 50% 40%;
  }

  .artwork-bottom {
    width: calc(100vw - 24px) !important;
    bottom: calc(env(safe-area-inset-bottom, 0px) + 66px) !important;
    padding: 8px 10px 6px !important;
    border-radius: 18px;
  }

  .artwork-bottom .led-mini {
    width: auto !important;
    height: clamp(58px, 10vh, 82px) !important;
    max-width: 70vw !important;
    margin-top: 2px;
  }

  .artwork-counter-label {
    font-size: 7px !important;
    letter-spacing: .18em !important;
  }

  .artwork-meta {
    margin-top: 1px !important;
    font-size: 7px !important;
  }

  .life-ticker {
    height: 24px !important;
    margin-top: 5px !important;
  }

  .life-ticker span {
    font-size: 7px !important;
  }

  .map-controls {
    right: 8px !important;
    bottom: 172px !important;
    transform: scale(.82);
    transform-origin: bottom right;
  }

  .tellium-dialog {
    width: calc(100vw - 22px) !important;
    max-height: calc(100dvh - 24px);
    overflow-y: auto;
  }
}

/* Très petits téléphones. */
@media (max-width: 390px) {
  .scene-artwork .tellium-canvas {
    transform: scale(.74) translateY(-7vh);
  }

  .artwork-bottom .led-mini {
    height: 54px !important;
  }

  .map-controls {
    bottom: 162px !important;
  }
}

/* Téléphone en mode paysage. */
@media (max-height: 520px) and (orientation: landscape) {
  .artwork-bottom {
    width: min(58vw, 560px) !important;
    bottom: 6px !important;
    padding: 4px 14px !important;
  }

  .artwork-bottom .led-mini {
    height: 48px !important;
  }

  .life-ticker {
    display: none;
  }

  .artwork-top-actions {
    top: 12px !important;
    right: 12px !important;
    left: auto !important;
    bottom: auto !important;
    width: auto;
    transform: none;
  }

  .scene-artwork .tellium-canvas {
    transform: scale(.86);
  }
}

/* ===== V17.1 — local clock, mobile controls and visible Earth ===== */

/* Les contrôles restent toujours au-dessus du compteur et du ticker. */
.map-controls {
  z-index: 96 !important;
  pointer-events: auto !important;
}

.map-controls .ctrl {
  pointer-events: auto !important;
}

/* Le slogan reste visible sur smartphone. */
@media (max-width: 760px) {
  .center-brand {
    top: 16px !important;
    width: 62vw !important;
    max-width: 290px;
  }

  .brand-name {
    font-size: 15px !important;
    letter-spacing: .32em !important;
  }

  .brand-tagline {
    display: block !important;
    margin-top: 7px !important;
    font-size: 6.5px !important;
    line-height: 1.5 !important;
    letter-spacing: .16em !important;
    white-space: nowrap;
    opacity: .76;
  }

  /*
   * La colonne + / − / World view / Find my light remonte
   * suffisamment pour ne plus passer derrière le compteur.
   */
  .map-controls {
    right: 9px !important;
    bottom: calc(env(safe-area-inset-bottom, 0px) + 230px) !important;
    transform: scale(.88) !important;
    transform-origin: bottom right !important;
    gap: 7px !important;
  }

  .map-controls .ctrl {
    width: 42px !important;
    height: 42px !important;
  }
}

/* Très petits écrans. */
@media (max-width: 390px) {
  .center-brand {
    width: 68vw !important;
  }

  .brand-tagline {
    font-size: 5.8px !important;
    letter-spacing: .13em !important;
  }

  .map-controls {
    bottom: calc(env(safe-area-inset-bottom, 0px) + 214px) !important;
    transform: scale(.82) !important;
  }
}

/* Smartphone en paysage : contrôles à droite, sans gêner le contenu. */
@media (max-height: 520px) and (orientation: landscape) {
  .map-controls {
    right: 10px !important;
    bottom: 10px !important;
    transform: scale(.76) !important;
  }

  .brand-tagline {
    display: block !important;
  }
}



/* ===== V17.2 — black globe harmony ===== */

/*
 * L’intérieur de l’œuvre reste noir.
 * Le bleu sert uniquement de lumière atmosphérique périphérique.
 */
.scene-artwork .cosmic-blue {
  opacity: .66;
  background:
    radial-gradient(
      ellipse at 50% 47%,
      transparent 0 34%,
      rgba(53, 117, 210, .045) 49%,
      rgba(60, 132, 230, .11) 62%,
      transparent 76%
    ),
    radial-gradient(
      ellipse at 50% 92%,
      rgba(51, 115, 205, .075),
      transparent 44%
    );
}

/*
 * L’horizon du bas est conservé,
 * mais sans les anneaux qui passaient derrière l’orbe central.
 */
.scene-home .rising-earth {
  filter: saturate(.88) brightness(.88);
}

/* Transition plus harmonieuse entre l’orbe noir et le globe réel. */
.scene-revealing .cosmic-blue {
  opacity: .48;
}

.scene-artwork .tellium-canvas {
  filter: saturate(.82) contrast(1.07) brightness(.91);
}


/* ===== V17.3 — coherent natural Earth ===== */

/*
 * On supprime totalement l’ancien décor synthétique.
 * C’est lui qui produisait les grandes courbes dorées derrière l’orbe.
 */
.rising-earth {
  display: none !important;
}

/*
 * La vraie image de la Terre devient le seul horizon de la Home.
 * Aucun anneau ni deuxième planète artificielle.
 */
.tellium-scene::after {
  content: "";
  position: fixed;
  z-index: 7;
  pointer-events: none;

  left: 0;
  right: 0;
  bottom: 0;
  height: min(36vh, 360px);

  background-image:
    linear-gradient(
      180deg,
      rgba(0, 0, 0, 0) 0%,
      rgba(0, 0, 0, 0.02) 48%,
      rgba(0, 0, 0, 0.14) 100%
    ),
    url("/earth-horizon.webp");

  background-position: center bottom;
  background-repeat: no-repeat;
  background-size: cover;

  opacity: 0;
  transition:
    opacity 1.1s ease,
    transform 1.8s cubic-bezier(.18,.75,.18,1);

  transform: translateY(7%);
}

/* Visible seulement sur la Home et pendant la transition. */
.scene-home::after {
  opacity: 1;
  transform: translateY(0);
}

.scene-revealing::after {
  opacity: .38;
  transform: translateY(4%);
}

.scene-artwork::after {
  opacity: 0;
  transform: translateY(10%);
}

/*
 * La Home 1 reste noire et or.
 * Aucun gros halo bleu au centre.
 */
.scene-home .cosmic-blue {
  opacity: .34 !important;
  background:
    radial-gradient(
      ellipse at 50% 94%,
      rgba(70, 142, 232, .075),
      transparent 38%
    ) !important;
}

/*
 * Home 2 : halo uniquement autour du globe.
 * Le centre ne devient pas bleu clair.
 */
.scene-artwork .cosmic-blue {
  opacity: .72 !important;
  background:
    radial-gradient(
      ellipse at 50% 48%,
      transparent 0 37%,
      rgba(54, 119, 210, .025) 44%,
      rgba(62, 135, 230, .065) 55%,
      transparent 72%
    ) !important;
}

/*
 * Ne plus assombrir excessivement le canvas :
 * le bleu nuit du globe reste perceptible.
 */
.scene-artwork .tellium-canvas {
  filter: saturate(.92) contrast(1.04) brightness(.98) !important;
}

/* Mobile : horizon légèrement moins haut pour préserver l’orbe. */
@media (max-width: 760px) {
  .tellium-scene::after {
    height: min(30vh, 250px);
    background-size: auto 100%;
  }
}
